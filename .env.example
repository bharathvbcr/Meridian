# No secrets are required to build or run Meridian.
# The AI assistant runs on-device via Gemini Nano (Firebase AI Logic), with an
# automatic cloud fallback configured through google-services.json — no API key needed.
#
# This file is retained as the Secrets Gradle plugin's default properties source.
