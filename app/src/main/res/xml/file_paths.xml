<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes the app cache dir where generated .ics invites are written for ACTION_SEND (§5.6). -->
    <cache-path name="ics_invites" path="invites/" />
</paths>
